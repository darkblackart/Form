/// <reference types="node" />
export default class StrykerMochaReporter {
    private runner;
    private runResult;
    private timer;
    private passedCount;
    constructor(runner: NodeJS.EventEmitter);
    private registerEvents();
}
