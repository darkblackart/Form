/**
 * Represents a handlebars result context
 */
export default class HandlebarsModel {
    title: string;
    urlPrefix: string;
    relativeUrl: string;
    totalKilled: number;
    totalTimedOut: number;
    totalSurvived: number;
    totalNoCoverage: number;
    totalErrors: number;
    constructor(title: string, urlPrefix: string, relativeUrl: string, totalKilled: number, totalTimedOut: number, totalSurvived: number, totalNoCoverage: number, totalErrors: number);
    totalDetected: number;
    totalUndetected: number;
    totalMutations: number;
    totalCoveredMutations: number;
    percentageBasedOnAllCode: number;
    percentageBasedOnCoveredCode: number;
}
