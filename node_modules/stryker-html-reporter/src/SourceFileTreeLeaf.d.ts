import { SourceFile, MutantResult } from 'stryker-api/report';
import HandlebarsModel from './HandlebarsModel';
export default class SourceFileTreeLeaf {
    file: SourceFile;
    results: MutantResult[];
    name: string;
    model: HandlebarsModel;
    filename: string;
    constructor(file: SourceFile, results?: MutantResult[]);
    writeFileReport(directory: string): void;
    calculateModel(urlPrefix: string): void;
}
